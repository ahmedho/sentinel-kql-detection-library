# Sentinel KQL Detection Library

🚧 Work in progress — part of a 12-week SC-200 / SOC engineering portfolio project.

## What this demonstrates
*(to be written once detections are built and verified)*

## Structure
- `detections/` — one `.kql` + `.yaml` per analytics rule
- `simulation/` — how the underlying telemetry was generated
- `docs/` — module write-ups, including errors encountered and key learnings
- `diagrams/` — architecture and data-flow diagrams

## Status
Not yet started — see `docs/` for progress as modules land.